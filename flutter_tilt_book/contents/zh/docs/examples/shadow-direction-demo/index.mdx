---
title: Shadow Direction
description:
keywords: ["documents", "示例", "Shadow Direction"]
---

<ExampleCodeWidget name="ShadowDirectionDemo" height={700}>
```dart
import 'package:flutter_tilt/flutter_tilt.dart';

······

Tilt(
  borderRadius: BorderRadius.circular(30),
  lightConfig: const LightConfig(disable: true),
  shadowConfig: ShadowConfig(direction: ShadowDirection.around),
  child: Container(
    width: 350,
    height: 200,
    alignment: Alignment.center,
    decoration: const BoxDecoration(
      gradient: LinearGradient(
        begin: Alignment.topCenter,
        end: Alignment.bottomCenter,
        colors: [Color(0xFF8989ba), Color(0xFFa7a6cb)],
      ),
    ),
    child: const Text('Flutter Tilt ✨', style: TextStyle(fontSize: 20, color: Colors.white)),
  ),
),

······
```
</ExampleCodeWidget>

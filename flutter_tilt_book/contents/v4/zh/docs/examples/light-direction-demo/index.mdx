---
title: Light Direction
description:
keywords: ["示例", "Light Direction"]
---

<ExampleCodeWidget pathName="LightDirectionDemo" height={700} previewText="预览" codeText="代码">
```dart
class TiltExample extends StatelessWidget {
  const TiltExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Tilt(
      child: TiltBaseContainer(
        borderRadius: BorderRadius.circular(30),
        lightConfig: LightConfig(direction: LightDirection.around),
        child: Container(
          width: 350,
          height: 200,
          alignment: Alignment.center,
          decoration: const BoxDecoration(
            gradient: LinearGradient(
              begin: Alignment.topCenter,
              end: Alignment.bottomCenter,
              colors: [Color(0xFF537895), Color(0xFF09203f)],
            ),
          ),
          child: const Text('Flutter Tilt ✨', style: TextStyle(fontSize: 20, color: Colors.white)),
        ),
      ),
    );
  }
}
```
</ExampleCodeWidget>

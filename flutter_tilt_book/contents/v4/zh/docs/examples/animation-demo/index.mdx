---
title: Animation
description:
keywords: ["示例", "Animation"]
---

<ExampleCodeWidget name="AnimationDemo" height={850} previewText="预览" codeText="代码">
```dart
import 'package:flutter_tilt/flutter_tilt.dart';

······

Tilt(
  borderRadius: BorderRadius.circular(30),
  tiltConfig: const TiltConfig(
    angle: 30,
    leaveDuration: Duration(seconds: 1),
    leaveCurve: Curves.bounceOut,
  ),
  childLayout: const ChildLayout(
    outer: [
      Positioned(
        child: TiltParallax(
          size: Offset(40, 40),
          child: Text('Flutter Tilt ✨', style: TextStyle(fontSize: 20, color: Colors.white)),
        ),
      ),
    ],
  ),
  child: Container(
    width: 350,
    height: 200,
    decoration: const BoxDecoration(
      gradient: LinearGradient(
        begin: Alignment.topRight,
        end: Alignment.bottomLeft,
        colors: [Color(0xFF80d0c7), Color(0xFF13547a)],
      ),
    ),
  ),
),

······
```
</ExampleCodeWidget>

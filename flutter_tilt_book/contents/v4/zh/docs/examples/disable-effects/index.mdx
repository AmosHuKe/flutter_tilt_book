---
title: Disable Effects
description:
keywords: ["示例", "Disable Effects"]
---

<ExampleCodeWidget pathName="DisableEffects" height={650} previewText="预览" codeText="代码">
```dart
class TiltExample extends StatelessWidget {
  const TiltExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Tilt(
      tiltConfig: TiltConfig(disable: false),
      child: TiltBaseContainer(
        lightConfig: LightConfig(disable: true),
        shadowConfig: ShadowBaseConfig(disable: true),
        borderRadius: BorderRadius.circular(30),
        child: Container(
          width: 350,
          height: 200,
          alignment: Alignment.center,
          color: Colors.blueAccent,
          child: const Text('Flutter Tilt ✨', style: TextStyle(fontSize: 20, color: Colors.white)),
        ),
      ),
    );
  }
}
```
</ExampleCodeWidget>

---
title: Shadow Direction
description:
keywords: ["示例", "Shadow Direction"]
---

<ExampleCodeWidget pathName="ShadowDirectionDemo" height={700} previewText="预览" codeText="代码">
```dart
class TiltExample extends StatelessWidget {
  const TiltExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Tilt(
      child: TiltBaseContainer(
        lightConfig: const LightConfig(disable: true),
        shadowConfig: ShadowBaseConfig(direction: ShadowDirection.around),
        borderRadius: BorderRadius.circular(30),
        child: Container(
          width: 350,
          height: 200,
          alignment: Alignment.center,
          decoration: const BoxDecoration(
            gradient: LinearGradient(
              begin: Alignment.topCenter,
              end: Alignment.bottomCenter,
              colors: [Color(0xFF8989ba), Color(0xFFa7a6cb)],
            ),
          ),
          child: const Text('Flutter Tilt ✨', style: TextStyle(fontSize: 20, color: Colors.white)),
        ),
      ),
    );
  }
}
```
</ExampleCodeWidget>

---
title: Simple usage
description: Simple examples to help you get started quickly.
keywords: ["guide", "simple usage"]
---

## Tilt

The [Tilt widget][Tilt widget parameters] only manages gesture state, sensor state, and animation state.  
You can combine the [TiltBaseContainer widget][TiltBaseContainer widget parameters] within the Tilt widget to wrap the default tilt, light, and shadow effects.  
Alternatively, you can use the built-in `Tilt.base` widget to achieve the same effects.  

For more information about TiltContainer, check out [TiltContainer][].  

```dart {7-8, 21} showLineNumbers
/// Import flutter_tilt
import 'package:flutter_tilt/flutter_tilt.dart';

...

/// Tilt with TiltBaseContainer for default effects
Tilt(
  child: TiltBaseContainer(
    child: SizedBox(
      width: 150,
      height: 300,
      child: DecoratedBox(
        decoration: const BoxDecoration(color: Colors.grey),
      ),
    ),
  ),
),

/// Tilt with built-in Tilt.base for default effects.
/// Tilt.base is a convenient widget that combines Tilt and TiltBaseContainer.
Tilt.base(
  child: SizedBox(
    width: 150,
    height: 300,
    child: DecoratedBox(
      decoration: const BoxDecoration(color: Colors.grey),
    ),
  ),
),

...

```

## Parallax

[TiltParallax widget][TiltParallax widget parameters] can only be used in the `Tilt` widget.

```dart {12, 20} showLineNumbers
/// Import flutter_tilt
import 'package:flutter_tilt/flutter_tilt.dart';

...

Tilt(
  child: TiltBaseContainer(
    childLayout: const ChildLayout(
      outer: [
        /// Parallax here
        Positioned(
          child: TiltParallax(
            child: Text('Parallax'),
          ),
        ),
        /// Parallax here
        Positioned(
          top: 20.0,
          left: 20.0,
          child: TiltParallax(
            offset: Offset(-10.0, -10.0),
            child: Text('Tilt'),
          ),
        ),
      ],
    ),
    child: SizedBox(
      width: 150,
      height: 300,
      child: DecoratedBox(
        decoration: const BoxDecoration(color: Colors.brown),
      ),
    ),
  ),
),

...

```

## More parameters

<NavCardGrid>
  <NavCard
    subtitle="Widget"
    title="Tilt Widget"
    description={
      <div>
        Learn about Tilt widget parameters.
        <br />
        Tilt widget only manages gesture state, sensor state, and animation state.
      </div>
    }
    href="/en/v4-rc/docs/widgets/tilt-widget"
  />
  <NavCard
    subtitle="Widget"
    title="TiltContainer"
    description={
      <div>
        Learn about TiltContainer styles.
      </div>
    }
    href="/en/v4-rc/docs/widgets/tilt-container"
  />
  <NavCard
    subtitle="Widget"
    title="TiltParallax Widget"
    description={
      <div>
        Learn about TiltParallax widget parameters.
        <br />
        Parallax effect in the Tilt widget.
      </div>
    }
    href="/en/v4-rc/docs/widgets/tilt-parallax-widget"
  />
</NavCardGrid>

[Tilt widget parameters]: /en/v4-rc/docs/widgets/tilt-widget
[TiltBaseContainer widget parameters]: /en/v4-rc/docs/widgets/tilt-base-container-widget
[TiltContainer]: /en/v4-rc/docs/widgets/tilt-container/
[TiltParallax widget parameters]: /en/v4-rc/docs/widgets/tilt-parallax-widget
